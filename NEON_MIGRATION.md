# Neon migration branch

This branch keeps the original production HTML files under `legacy/` and replaces only the active entry pages with lightweight Neon loaders. `main` is unchanged and remains the rollback point.
